import SwiftUI

struct Tokens {
    static let colors: Colors = Colors()
    static let fonts: Fonts = Fonts()
    static let paddings: Paddings = Paddings()
    static let sizeComponents: SizeComponents = SizeComponents()
}
